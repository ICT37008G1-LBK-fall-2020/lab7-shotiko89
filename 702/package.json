{
  "name": "task2",
  "version": "1.0.0",
  "description": "task2 desciption",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify --entry index.js --outfile bundle.js"
  },
  "author": "shotiko tsurtsumia",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^17.0.0"
  }
}
